# Core dependencies
pyyaml>=6.0
openpyxl>=3.1.0

# Hallucination analyzer - universe construction
# pdftotext (poppler-utils) is also required at the system level.
# Without easyocr+pdf2image, N2-N5 scanned PDFs fall out of the universe
# and fabrication rates inflate by several points across all models.
easyocr>=1.7
pdf2image>=1.16
numpy>=1.26
scipy>=1.11

# Model providers (install as needed)
openai>=1.0
anthropic>=0.96                # need >=0.96 for thinking.type=adaptive + output_config
google-genai>=1.70             # replaces deprecated google-generativeai; needed for ThinkingConfig
