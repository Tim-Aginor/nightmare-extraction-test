# Core dependencies
pyyaml>=6.0
openpyxl>=3.1.0

# Note: hallucination_analyzer used to require poppler-utils + easyocr +
# pdf2image + numpy + scipy for live PDF/xlsx/csv parsing. Dropped
# 2026-05-08 (plan §4-O) — universe is now built from the generator's
# JSON ground-truth artifacts, no source-document parsing.

# Model providers (install as needed)
openai>=1.0
anthropic>=0.96                # need >=0.96 for thinking.type=adaptive + output_config
google-genai>=1.70             # replaces deprecated google-generativeai; needed for ThinkingConfig
